{
  "name": "as-web-component",
  "version": "0.3.0",
  "description": "Web Components from functions",
  "main": "src/asWebComponent.js",
  "module": "src/asWebComponent.js",
  "scripts": {
    "test": "npm run lint && npm run build",
    "start": "esbuild example/main.js --bundle --outdir=example/dist --jsx-import-source=preact --jsx=automatic --servedir=example",
    "build": "rollup -c --compact",
    "lint": "eslint src test example"
  },
  "keywords": [
    "web",
    "component",
    "pure",
    "function"
  ],
  "author": "David Beale",
  "license": "MIT",
  "dependencies": {
    "fn-args": "^5.0.0",
    "param-case": "^3.0.3",
    "uid": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.15.16",
    "eslint": "^8.23.1",
    "eslint-config-airbnb": "^19.0.4",
    "preact": "^10.11.3"
  }
}
